import { CredentialsVerifier, CredentialFormat, OrgResolver, SDKCacheManager } from '../inji-verify-sdk/src/index.js';
import fs from 'fs';
import fetch from 'node-fetch';

const vc = JSON.parse(`PLACEHOLDER`);
